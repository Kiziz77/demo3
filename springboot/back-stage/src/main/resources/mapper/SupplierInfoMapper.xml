<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.mapper.SupplierInfoMapper">


    <select id="selectRespList" resultType="com.web.domain.Resp.SupplierInfoResp">
        select
            t1.`id`,
            t1.`supplier_name`,
            t1.`contact_person`,
            t1.`phone`,
            t1.`address`
        from supplier_info t1
        <where>
                            <if test="req.supplierName != null and req.supplierName != ''">
                                and t1.supplier_name like concat('%', #{req.supplierName}, '%')
                            </if>
                            <if test="req.contactPerson != null and req.contactPerson != ''">
                                and t1.contact_person like concat('%', #{req.contactPerson}, '%')
                            </if>
                            <if test="req.phone != null and req.phone != ''">
                                and t1.phone like concat('%', #{req.phone}, '%')
                            </if>
                            <if test="req.address != null and req.address != ''">
                                and t1.address like concat('%', #{req.address}, '%')
                            </if>
        </where>
    </select>
</mapper>
