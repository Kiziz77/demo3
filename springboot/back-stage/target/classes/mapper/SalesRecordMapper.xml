<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.mapper.SalesRecordMapper">


    <select id="selectRespList" resultType="com.web.domain.Resp.SalesRecordResp">
        select
            t1.`id`,
            t1.`inventory_id`,
            t1.`sales_quantity`,
            t1.`sales_time`,
            t1.`customer_name`
                        ,t2.`drug_id`   t2Drugid
        from sales_record t1
                        left join inventory t2 on t1.inventory_id = t2.id

        <where>
                            <if test="req.inventoryId != null">
                                and t1.inventory_id = #{req.inventoryId}
                            </if>
                            <if test="req.salesQuantity != null">
                                and t1.sales_quantity = #{req.salesQuantity}
                            </if>
                            <if test="req.customerName != null and req.customerName != ''">
                                and t1.customer_name like concat('%', #{req.customerName}, '%')
                            </if>
        </where>
    </select>
</mapper>
